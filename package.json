{
  "name": "@graphql-detective/root",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -filter \"@graphql-detective/core\" build",
    "check": "biome check --write",
    "dev": "rslib build --watch",
    "format": "biome format --write",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.3"
  },
  "private": true
}
